import {
  equipArmorPiece,
  grantArmorToSession,
  grantBackpackItemToSession,
  grantToolToSession,
} from "../mechanics/itemlogic";
import { saveMainSaveToStorage } from "../mechanics/mainsavelogic";
import { sessionMainSave } from "./sessionmainsave";

const toTest = () => {
  // console.log(sessionMainSave.value.inventory.equippedTools);
  // grantArmorToSession("helmet", 1, 1);
  // grantToolToSession("smithingkit", 1, 1);
  // grantToolToSession("cookingkit", 1, 1);
  // grantToolToSession("fishingrod", 1, 1);
  // grantToolToSession("basket", 1, 1);
  // grantToolToSession("hoe", 1, 1);
  // grantBackpackItemToSession("01_000001", 99);
  // grantBackpackItemToSession("02_000002", 1);
  // grantBackpackItemToSession("05_000001", 1);
  // grantBackpackItemToSession("03_000001", 1);
  // grantBackpackItemToSession("03_000002", 1);
  // grantBackpackItemToSession("04_000001", 1);
  // grantBackpackItemToSession("04_000002", 1);
  // saveMainSaveToStorage();
  // equipArmorPiece(0);
  // sessionMainSave.value.inventory.properties.inventoryLevel = 18;
  // console.log(sessionMainSave.value.inventory);
  // saveMainSaveToStorage();
};

toTest();
